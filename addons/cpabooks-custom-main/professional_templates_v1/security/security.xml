<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="cpa_signature_rule" model="ir.rule">
        <field name="name">Signature Rule</field>
        <field name="model_id" ref="model_signature_setup"/>
        <field name="global" eval="True" />
        <field name="domain_force">user.signature_domain()</field>
    </record>

</odoo>